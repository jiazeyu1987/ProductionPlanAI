# 6子Agent超大文件拆分总控说明（2026-03-29）

## 目标
对以下6个超大文件并行做第一轮拆分，降低复杂度、耦合度，保证可扩展可维护，且保持S0（外部行为与契约不变）：
1. `code/backend/src/main/java/com/autoproduction/mvp/core/ErpSqliteOrderLoader.java`（约2300行）
2. `code/backend/src/main/java/com/autoproduction/mvp/module/integration/erp/ErpDataManager.java`（约827行）
3. `code/frontend/src/pages/LiteSchedulerPage.jsx`（约2631行）
4. `code/frontend/src/pages/ScheduleCalendarPage.jsx`（约1265行）
5. `code/frontend/src/pages/MasterdataPage.jsx`（约1177行）
6. `code/frontend/src/pages/OrdersPoolPage.jsx`（约945行）

## 并行规则（必须遵守）
1. 每个子Agent只允许修改自己的归属文件和归属目录，禁止跨界改动。
2. 不允许回滚他人修改。
3. 每完成一个切片就跑测试验证；测试失败必须先修复再继续下个切片。
4. 优先抽离纯函数、映射、解析、状态转换；避免一次性大爆改。
5. 不改API契约、不改外部路由、不改数据库结构。

## 统一门禁
后端子Agent：
1. `cd code/backend && mvn -q -DskipTests compile`
2. `cd code/backend && mvn -q -Dtest=MvpApiTest test`（不可执行需注明原因）

前端子Agent：
1. `cd code/frontend && npm test -- --run`
2. `cd code/frontend && npm run build`

## 集成顺序（由总负责人执行）
1. 先集成后端 A/B。
2. 再集成前端 C/D/F（LiteScheduler/ScheduleCalendar/OrdersPool）。
3. 最后集成前端 E（Masterdata，依赖较多，放最后降低冲突）。
4. 完成全量回归：
   - `cd code/backend && mvn test`
   - `cd code/frontend && npm test -- --run && npm run build`

## 子Agent提示词文件
1. `A_backend_erp_loader_prompt.md`
2. `B_backend_erp_data_manager_prompt.md`
3. `C_frontend_lite_scheduler_prompt.md`
4. `D_frontend_schedule_calendar_prompt.md`
5. `E_frontend_masterdata_prompt.md`
6. `F_frontend_orders_pool_prompt.md`

## 子Agent交付模板
每个子Agent最终必须提供：
1. 变更文件清单。
2. 主文件行数变化（before/after）。
3. 拆分设计说明（提取了哪些职责、为何这样拆）。
4. 测试命令与结果。
5. 风险与下一步建议。
