# Refactor Test Record (2026-03-29)

1. Refactor step id: `FE-G-02`
2. Wave: `Wave-1 / PLAT-03`
3. Changed module: `frontend shared API`
4. Changed files:
   1. `code/frontend/src/shared/api/requestCore.js`
   2. `code/frontend/src/shared/api/contractClient.js`
   3. `code/frontend/src/shared/api/legacyClient.js`
   4. `code/frontend/src/shared/api/fileClient.js`
   5. `code/frontend/src/services/api.js`

## FE-G-02 (Split api.js into request core + domain clients)
5. Change summary:
   1. Added `requestCore` as shared low-level request implementation for JSON/blob requests.
   2. Added domain clients:
      - `contractClient` for contract-authenticated requests
      - `legacyClient` for non-contract legacy requests
      - `fileClient` for contract file download flow
   3. Kept `services/api.js` as compatibility facade with unchanged public methods:
      - `apiRequest`, `loadList`, `postContract`, `postLegacy`, `downloadContractFile`
   4. No route/payload semantic change (`S0`), only dependency layering and coupling reduction.

6. Tests executed:
   1. `cd code/frontend && npm test -- --run`
   2. `cd code/frontend && npm run build`

7. Test results:
   1. `vitest`: `6 files passed / 49 tests passed / 0 failed`
   2. `vite build`: success

8. Risk and rollback:
   1. Low-to-medium risk (shared request layer touched).
   2. Rollback by reverting the 5 files listed above.

9. Allow next step: `YES`

