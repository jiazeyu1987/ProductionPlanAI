# Refactor Test Record (2026-03-29)

1. Refactor step id: `FE-C-01` (slice-27)
2. Wave: `Wave-1 / PLAT-03`
3. Changed module: `frontend masterdata`
4. Changed files:
   1. `code/frontend/src/features/masterdata/optionStateUtils.js`
   2. `code/frontend/src/features/masterdata/index.js`
   3. `code/frontend/src/pages/MasterdataPage.jsx`

## FE-C-01 (Masterdata option-state decision helper extraction)
5. Change summary:
   1. Added generic option-state helper `resolveSelectableCodeUpdate` to unify repeated selection-correction logic.
   2. Re-exported helper and switched three duplicated `useEffect` blocks in `MasterdataPage` to helper-based implementation.
   3. Preserved existing behavior (`S0`): empty-options clear rule, invalid-current fallback rule, and no-op when current code exists.

6. Tests executed:
   1. `cd code/frontend && npm test -- --run`
   2. `cd code/frontend && npm run build`

7. Test results:
   1. `vitest`: `6 files passed / 49 tests passed / 0 failed`
   2. `vite build`: success

8. Risk and rollback:
   1. Low risk (dedup of existing branch logic).
   2. Rollback by reverting the 3 files listed above.

9. Allow next step: `YES`
