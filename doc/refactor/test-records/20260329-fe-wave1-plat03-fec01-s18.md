# Refactor Test Record (2026-03-29)

1. Refactor step id: `FE-C-01` (slice-18)
2. Wave: `Wave-1 / PLAT-03`
3. Changed module: `frontend masterdata`
4. Changed files:
   1. `code/frontend/src/features/masterdata/routeListUtils.js`
   2. `code/frontend/src/features/masterdata/index.js`
   3. `code/frontend/src/pages/MasterdataPage.jsx`

## FE-C-01 (Masterdata route-list utility extraction)
5. Change summary:
   1. Extracted route-list display/filter/group helpers from `MasterdataPage` into `routeListUtils`:
      - `resolveRouteListProductName`
      - `resolveRouteListDisplayCode`
      - `dependencyTypeLabel`
      - `isRouteVisibleInList`
      - `sortRoutesForList`
      - `buildGroupedRouteRowsForList`
   2. Re-exported new route helper module and switched `MasterdataPage` route-tab wiring to consume extracted helpers.
   3. Kept route/product naming mapping and hidden-code policy unchanged (`S0`), only moved execution location.

6. Tests executed:
   1. `cd code/frontend && npm test -- --run`
   2. `cd code/frontend && npm run build`

7. Test results:
   1. `vitest`: `6 files passed / 49 tests passed / 0 failed`
   2. `vite build`: success

8. Risk and rollback:
   1. Low risk (list helper extraction with stable API behavior).
   2. Rollback by reverting the 3 files listed above.

9. Allow next step: `YES`
