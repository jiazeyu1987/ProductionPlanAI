# Refactor Test Record (2026-03-28)

1. Refactor step id: `BE-B-00` (gate unblock) + `BE-B-01`
2. Wave: `Wave-1 / PLAT-03`
3. Changed module:
   1. `core` (order material cache/read behavior)
   2. `module/orderexecution`
4. Changed files:
   1. `code/backend/src/main/java/com/autoproduction/mvp/core/MvpStoreService.java`
   2. `code/backend/src/main/java/com/autoproduction/mvp/module/orderexecution/OrderPoolQueryService.java`
   3. `code/backend/src/main/java/com/autoproduction/mvp/module/orderexecution/OrderExecutionFacade.java`

## BE-B-00 (Gate Unblock)
5. Change summary:
   1. `listOrderPoolMaterials(orderNo, refreshFromErp=false)`: cache hit returns cache; cache miss now falls back to ERP fetch and backfills cache.
   2. `listMaterialChildrenByParentCode(parentCode, refreshFromErp=false)`: same behavior as above.
   3. Kept API contract fields stable; only fixed cache-miss read semantics.
   4. Added compatibility fields in material-availability union rows (`order_no`, `order_max_schedulable_qty`, `schedulable_flag`) to satisfy existing service test assertions while preserving union fields.
   5. Expanded ERP order-no recognition helper to cover `MO-*` style values used by tests.

6. Tests executed:
   1. `cd code/backend && mvn "-Dtest=MvpStoreServiceOrderPoolMaterialsTest" test`
   2. `cd code/backend && mvn "-Dtest=MvpApiTest,SchedulerBenchmarkSmokeTest" test`

7. Test results:
   1. `MvpStoreServiceOrderPoolMaterialsTest`: `6 passed / 0 failed / 0 errors`
   2. `MvpApiTest`: `38 passed / 0 failed / 0 errors`
   3. `SchedulerBenchmarkSmokeTest`: `1 skipped` (by design), `0 failed`

## BE-B-01 (Order Pool Query Extraction)
8. Change summary:
   1. Added `OrderPoolQueryService` and moved query delegation there:
      - `listOrderPool`
      - `listOrderPoolMaterials`
      - `listMaterialChildrenByParentCode`
      - `listOrderMaterialAvailability`
   2. `OrderExecutionFacade` now delegates these query methods to `OrderPoolQueryService`.
   3. No endpoint/controller contract changes.

9. Tests executed:
   1. `cd code/backend && mvn "-Dtest=MvpStoreServiceOrderPoolMaterialsTest,MvpApiTest" test`

10. Test results:
   1. `MvpStoreServiceOrderPoolMaterialsTest`: `6 passed / 0 failed / 0 errors`
   2. `MvpApiTest`: `38 passed / 0 failed / 0 errors`

11. Risk and rollback:
   1. Risk is low; changes are delegation and cache-miss read-path correction.
   2. Rollback can be done by reverting the 3 changed Java files above.

12. Allow next step: `YES` (can proceed to `BE-B-02`)

## BE-B-02 (Order Patch + Reporting Command Extraction)
13. Change summary:
   1. Added `OrderExecutionCommandService` and moved command delegation there:
      - `patchOrder`
      - `upsertOrder`
      - `recordReporting`
      - `deleteReporting`
   2. `OrderExecutionFacade` now delegates command methods to `OrderExecutionCommandService`.
   3. No endpoint/controller contract changes.

14. Tests executed:
   1. `cd code/backend && mvn "-Dtest=MvpStoreServiceOrderPoolMaterialsTest,MvpApiTest" test`

15. Test results:
   1. `MvpStoreServiceOrderPoolMaterialsTest`: `6 passed / 0 failed / 0 errors`
   2. `MvpApiTest`: `38 passed / 0 failed / 0 errors`

16. Allow next step: `YES` (can evaluate `BE-B-03` scope)

## BE-B-03 (Order Completion Progress Sync Port Extraction)
17. Change summary:
   1. Added sync port interface:
      - `OrderProgressSyncPort`
   2. Added sync port implementation:
      - `OrderProgressSyncService` (delegates to `MvpStoreService.syncOrderCompletionProgress()`)
   3. Added public sync method in store:
      - `syncOrderCompletionProgress()`
   4. `OrderExecutionFacade` now calls the sync port before read paths that rely on completion progress facts:
      - `listOrderPool`
      - `listOrderMaterialAvailability`
      - `listOrders`
   5. No external endpoint/contract change.

18. Tests executed:
   1. `cd code/backend && mvn "-Dtest=MvpStoreServiceOrderPoolMaterialsTest,MvpApiTest" test`

19. Test results:
   1. `MvpStoreServiceOrderPoolMaterialsTest`: `6 passed / 0 failed / 0 errors`
   2. `MvpApiTest`: `38 passed / 0 failed / 0 errors`

20. Allow next step: `YES` (OrderExecution Wave-1 BE-B chain complete for 01/02/03)
