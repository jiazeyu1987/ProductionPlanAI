# Refactor Test Record (2026-03-29)

1. Refactor step id: `FE-C-01` (slice-57)
2. Wave: `Wave-1 / PLAT-03`
3. Changed module: `frontend masterdata`
4. Changed files:
   1. `code/frontend/src/features/masterdata/lineTopologyUtils.js`
   2. `code/frontend/src/features/masterdata/lineTopologyUtils.test.js`

## FE-C-01 (Masterdata shared line-topology row builder extraction)
5. Change summary:
   1. Added `buildLineTopologyProcessRow(params, options)` in `lineTopologyUtils` as a shared row-construction utility.
   2. Refactored `buildTopologyLineProcessRow` to delegate to the new shared builder with `{ requireProcess: true }`, preserving existing semantics.
   3. Added utility unit test for the new shared builder.
   4. Preserved behavior (`S0`): existing `buildTopologyLineProcessRow` behavior and current page logic unchanged.

6. Tests executed:
   1. `cd code/frontend && npm test -- --run`
   2. `cd code/frontend && npm run build`

7. Test results:
   1. `vitest`: `8 files passed / 71 tests passed / 0 failed`
   2. `vite build`: success

8. Risk and rollback:
   1. Low risk (utility extraction only).
   2. Rollback by reverting the 2 files listed above.

9. Allow next step: `YES`
