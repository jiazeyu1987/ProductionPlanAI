# Refactor Test Record (2026-03-29)

1. Refactor step id: `FE-C-01` (slice-15)
2. Wave: `Wave-1 / PLAT-03`
3. Changed module: `frontend masterdata`
4. Changed files:
   1. `code/frontend/src/features/masterdata/codeNormalizeUtils.js`
   2. `code/frontend/src/features/masterdata/index.js`
   3. `code/frontend/src/pages/MasterdataPage.jsx`

## FE-C-01 (Masterdata code-normalize utility extraction + syntax stabilization)
5. Change summary:
   1. Extracted code-normalization/grouping helpers from `MasterdataPage` into `codeNormalizeUtils`:
      - `routeGroupKey`
      - `normalizeShiftCode`
      - `shiftSortIndex`
      - `normalizeProcessCode`
      - `normalizeCompanyCode`
      - `normalizeProductCode`
      - `normalizeDependencyType`
   2. Re-exported the new utility module from feature index and switched `MasterdataPage` to import these helpers.
   3. During the extraction checkpoint, repaired multiple broken string/template literals in `MasterdataPage` caused by encoding corruption so the module can compile and execute stably.
   4. Kept API contract and data write/read paths unchanged (`S0`), only normalization utility ownership and UI message text were stabilized.

6. Tests executed:
   1. `cd code/frontend && npm test -- --run`
   2. `cd code/frontend && npm run build`

7. Test results:
   1. `vitest`: `6 files passed / 49 tests passed / 0 failed`
   2. `vite build`: success

8. Risk and rollback:
   1. Medium-low risk: utility extraction is low risk, but this slice also includes syntax recovery in a large page file.
   2. Rollback by reverting the 3 files listed above.

9. Allow next step: `YES`
