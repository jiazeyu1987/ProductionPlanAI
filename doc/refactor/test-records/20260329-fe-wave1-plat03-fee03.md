# Refactor Test Record (2026-03-29)

1. Refactor step id: `FE-E-03` (slice-1)
2. Wave: `Wave-1 / PLAT-03`
3. Changed module: `frontend dashboard/integration`
4. Changed files:
   1. `code/frontend/src/features/dashboard/apiClient.js`
   2. `code/frontend/src/features/dashboard/index.js`
   3. `code/frontend/src/features/dashboard/README.md`
   4. `code/frontend/src/features/integration/apiClient.js`
   5. `code/frontend/src/pages/DashboardPage.jsx`
   6. `code/frontend/src/pages/PlanReportsPage.jsx`

## FE-E-03 (Dashboard integration card migration to feature wrappers)
5. Change summary:
   1. Added dashboard feature API wrappers for:
      - order-pool summary
      - open alerts summary
      - schedule versions summary
      - shift process load by version
   2. `DashboardPage` no longer imports `services/api` directly; now uses feature wrappers.
   3. Added report-page wrapper methods in `integration/apiClient`:
      - `listPlanReportScheduleVersions`
      - `listPlanReportOrderPool`
   4. `PlanReportsPage` no longer imports `services/api` directly.
   5. No route/payload semantic change (`S0`), only dependency boundary optimization.

6. Tests executed:
   1. `cd code/frontend && npm test -- --run`
   2. `cd code/frontend && npm run build`

7. Test results:
   1. `vitest`: `6 files passed / 49 tests passed / 0 failed`
   2. `vite build`: success

8. Risk and rollback:
   1. Low risk (API call entry points moved to feature wrappers; behavior unchanged).
   2. Rollback by reverting the 6 files listed above.

9. Allow next step: `YES`

