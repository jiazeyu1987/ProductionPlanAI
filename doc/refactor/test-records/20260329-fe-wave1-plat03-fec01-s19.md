# Refactor Test Record (2026-03-29)

1. Refactor step id: `FE-C-01` (slice-19)
2. Wave: `Wave-1 / PLAT-03`
3. Changed module: `frontend masterdata`
4. Changed files:
   1. `code/frontend/src/features/masterdata/routeEditorDataUtils.js`
   2. `code/frontend/src/features/masterdata/index.js`
   3. `code/frontend/src/pages/MasterdataPage.jsx`

## FE-C-01 (Masterdata route-editor data utility extraction)
5. Change summary:
   1. Extracted route-editor source data builder from `MasterdataPage` into `routeEditorDataUtils`:
      - `buildRouteStepsByProduct`
   2. Re-exported the new utility in `features/masterdata/index.js` and switched the page memo to call extracted helper.
   3. Kept route-step grouping and ordering semantics unchanged (`S0`): same product normalization, dependency normalization, and `sequence_no` ordering logic.

6. Tests executed:
   1. `cd code/frontend && npm test -- --run`
   2. `cd code/frontend && npm run build`

7. Test results:
   1. `vitest`: `6 files passed / 49 tests passed / 0 failed`
   2. `vite build`: success

8. Risk and rollback:
   1. Low risk (pure data-build helper extraction with unchanged call site behavior).
   2. Rollback by reverting the 3 files listed above.

9. Allow next step: `YES`
