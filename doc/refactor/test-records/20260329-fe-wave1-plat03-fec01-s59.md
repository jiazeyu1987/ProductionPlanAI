# Refactor Test Record (2026-03-29)

1. Refactor step id: `FE-C-01` (slice-59)
2. Wave: `Wave-1 / PLAT-03`
3. Changed module: `frontend masterdata`
4. Changed files:
   1. `code/frontend/src/pages/MasterdataPage.jsx`

## FE-C-01 (Masterdata topology add-workshop/add-line shared builder unification)
5. Change summary:
   1. Refactored `addTopologyWorkshop` to use `buildLineTopologyProcessRow(...)` instead of a page-local literal row object.
   2. Refactored `addTopologyLine` to use `buildLineTopologyProcessRow(...)` and removed redundant local line-index name assembly.
   3. Removed now-unused `matchesTopologyWorkshop` import after the above simplification.
   4. Recovered `MasterdataPage.jsx` Chinese text encoding (mojibake repair) to keep UI copy and route-product mapping deterministic.
   5. Preserved behavior (`S0`) for topology insertion semantics and default process selection.

6. Tests executed:
   1. `cd code/frontend && npm test -- --run`
   2. `cd code/frontend && npm run build`

7. Test results:
   1. `vitest`: `8 files passed / 71 tests passed / 0 failed`
   2. `vite build`: success

8. Risk and rollback:
   1. Low-medium risk (page-level mutation path unification + file encoding recovery).
   2. Rollback by reverting the file listed above.

9. Allow next step: `YES`
