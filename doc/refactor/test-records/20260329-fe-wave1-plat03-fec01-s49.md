# Refactor Test Record (2026-03-29)

1. Refactor step id: `FE-C-01` (slice-49)
2. Wave: `Wave-1 / PLAT-03`
3. Changed module: `frontend masterdata`
4. Changed files:
   1. `code/frontend/src/features/masterdata/lineTopologyUtils.js`
   2. `code/frontend/src/features/masterdata/lineTopologyUtils.test.js`
   3. `code/frontend/src/pages/MasterdataPage.jsx`

## FE-C-01 (Masterdata topology workshop/line remove helper extraction)
5. Change summary:
   1. Added helpers in `lineTopologyUtils`:
      - `filterOutTopologyWorkshopRows(rows, workshop)`
      - `filterOutTopologyLineRows(rows, line)`
   2. Replaced page-local inline filters in:
      - `removeTopologyWorkshop`
      - `removeTopologyLine`
   3. Added unit tests for workshop-level and line-level filtering semantics.
   4. Preserved existing behavior (`S0`): matching keys and normalization rules unchanged.

6. Tests executed:
   1. `cd code/frontend && npm test -- --run`
   2. `cd code/frontend && npm run build`

7. Test results:
   1. `vitest`: `8 files passed / 62 tests passed / 0 failed`
   2. `vite build`: success

8. Risk and rollback:
   1. Low risk (duplicate filter extraction).
   2. Rollback by reverting the 3 files listed above.

9. Allow next step: `YES`
