# Refactor Test Record (2026-03-29)

1. Refactor step id: `FE-C-01` (slice-37)
2. Wave: `Wave-1 / PLAT-03`
3. Changed module: `frontend masterdata`
4. Changed files:
   1. `code/frontend/src/features/masterdata/dailyConfigRowsUtils.js`
   2. `code/frontend/src/pages/MasterdataPage.jsx`

## FE-C-01 (Masterdata resource/material row-key helper extraction)
5. Change summary:
   1. Added row-key helpers in `dailyConfigRowsUtils`:
      - `buildResourceRowKey`
      - `buildMaterialRowKey`
   2. Replaced page-local row key functions and all call sites for update/add/remove checks with helper calls.
   3. Preserved existing behavior (`S0`): key composition fields and normalization order unchanged.

6. Tests executed:
   1. `cd code/frontend && npm test -- --run`
   2. `cd code/frontend && npm run build`

7. Test results:
   1. `vitest`: `6 files passed / 49 tests passed / 0 failed`
   2. `vite build`: success

8. Risk and rollback:
   1. Low risk (pure key-helper extraction).
   2. Rollback by reverting the 2 files listed above.

9. Allow next step: `YES`
