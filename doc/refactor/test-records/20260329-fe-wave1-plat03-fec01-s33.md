# Refactor Test Record (2026-03-29)

1. Refactor step id: `FE-C-01` (slice-33)
2. Wave: `Wave-1 / PLAT-03`
3. Changed module: `frontend masterdata`
4. Changed files:
   1. `code/frontend/src/features/masterdata/dailyConfigRowsUtils.js`
   2. `code/frontend/src/pages/MasterdataPage.jsx`

## FE-C-01 (Masterdata night-row ensure logic extraction)
5. Change summary:
   1. Added `ensureNightRowsInConfigData` into `dailyConfigRowsUtils` to encapsulate night-row补齐规则（resource/material）。
   2. Replaced page-side `ensureNightRowsForDate` heavy state-update body with helper call.
   3. Preserved existing behavior (`S0`): same day-row scan scope, same dedup keys, same inserted field values, and same unchanged-return behavior.

6. Tests executed:
   1. `cd code/frontend && npm test -- --run`
   2. `cd code/frontend && npm run build`

7. Test results:
   1. `vitest`: `6 files passed / 49 tests passed / 0 failed`
   2. `vite build`: success

8. Risk and rollback:
   1. Low risk (pure updater extraction).
   2. Rollback by reverting the 2 files listed above.

9. Allow next step: `YES`
