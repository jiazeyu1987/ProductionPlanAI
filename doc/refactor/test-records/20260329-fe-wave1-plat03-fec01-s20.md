# Refactor Test Record (2026-03-29)

1. Refactor step id: `FE-C-01` (slice-20)
2. Wave: `Wave-1 / PLAT-03`
3. Changed module: `frontend masterdata`
4. Changed files:
   1. `code/frontend/src/features/masterdata/processOptionUtils.js`
   2. `code/frontend/src/features/masterdata/index.js`
   3. `code/frontend/src/pages/MasterdataPage.jsx`

## FE-C-01 (Masterdata process-option utility extraction)
5. Change summary:
   1. Extracted process option derivation from `MasterdataPage` into `processOptionUtils`:
      - `buildProcessOptions`
      - `buildProcessNameByCode`
   2. Re-exported new process option utility module and switched `MasterdataPage` memos to call extracted helpers.
   3. Preserved existing fallback and sorting behavior (`S0`): route-first source, topology fallback when routes are empty, Chinese locale name sort.

6. Tests executed:
   1. `cd code/frontend && npm test -- --run`
   2. `cd code/frontend && npm run build`

7. Test results:
   1. `vitest`: `6 files passed / 49 tests passed / 0 failed`
   2. `vite build`: success

8. Risk and rollback:
   1. Low risk (pure memo extraction with unchanged call graph and outputs).
   2. Rollback by reverting the 3 files listed above.

9. Allow next step: `YES`
