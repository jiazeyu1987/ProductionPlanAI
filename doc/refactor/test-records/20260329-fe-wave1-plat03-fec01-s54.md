# Refactor Test Record (2026-03-29)

1. Refactor step id: `FE-C-01` (slice-54)
2. Wave: `Wave-1 / PLAT-03`
3. Changed module: `frontend masterdata`
4. Changed files:
   1. `code/frontend/src/features/masterdata/lineTopologyUtils.js`
   2. `code/frontend/src/features/masterdata/lineTopologyUtils.test.js`
   3. `code/frontend/src/pages/MasterdataPage.jsx`

## FE-C-01 (Masterdata topology line-process row add/remove helper extraction)
5. Change summary:
   1. Added helpers in `lineTopologyUtils`:
      - `buildTopologyLineProcessRow(line, processCode)`
      - `filterOutTopologyLineProcessRows(rows, line, processCode)`
   2. Replaced `setTopologyLineProcess` in-page inline row build and remove filter with helper calls.
   3. Added unit tests for helper output and target line/process removal behavior.
   4. Preserved existing behavior (`S0`): field defaults and normalization rules unchanged.

6. Tests executed:
   1. `cd code/frontend && npm test -- --run`
   2. `cd code/frontend && npm run build`

7. Test results:
   1. `vitest`: `8 files passed / 68 tests passed / 0 failed`
   2. `vite build`: success

8. Risk and rollback:
   1. Low risk (pure helper extraction).
   2. Rollback by reverting the 3 files listed above.

9. Allow next step: `YES`
