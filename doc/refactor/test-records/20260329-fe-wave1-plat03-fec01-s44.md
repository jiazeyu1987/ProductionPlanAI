# Refactor Test Record (2026-03-29)

1. Refactor step id: `FE-C-01` (slice-44)
2. Wave: `Wave-1 / PLAT-03`
3. Changed module: `frontend masterdata`
4. Changed files:
   1. `code/frontend/src/features/masterdata/sectionLeaderUtils.js`
   2. `code/frontend/src/features/masterdata/sectionLeaderUtils.test.js`
   3. `code/frontend/src/pages/MasterdataPage.jsx`

## FE-C-01 (Masterdata section leader sort helper extraction)
5. Change summary:
   1. Added `sortSectionLeaderBindingRows(rows)` in `sectionLeaderUtils`.
   2. Replaced page-local inline sort comparator in `sectionLeaderRows` with helper call.
   3. Added utility unit test for sort order (`leader_id` then `line_code`).
   4. Preserved existing behavior (`S0`): locale and compare fields unchanged.

6. Tests executed:
   1. `cd code/frontend && npm test -- --run`
   2. `cd code/frontend && npm run build`

7. Test results:
   1. `vitest`: `8 files passed / 57 tests passed / 0 failed`
   2. `vite build`: success

8. Risk and rollback:
   1. Low risk (pure sort-helper extraction).
   2. Rollback by reverting the 3 files listed above.

9. Allow next step: `YES`
