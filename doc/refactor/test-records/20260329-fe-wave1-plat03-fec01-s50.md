# Refactor Test Record (2026-03-29)

1. Refactor step id: `FE-C-01` (slice-50)
2. Wave: `Wave-1 / PLAT-03`
3. Changed module: `frontend masterdata`
4. Changed files:
   1. `code/frontend/src/features/masterdata/lineTopologyUtils.js`
   2. `code/frontend/src/features/masterdata/lineTopologyUtils.test.js`
   3. `code/frontend/src/pages/MasterdataPage.jsx`

## FE-C-01 (Masterdata topology line update helper extraction)
5. Change summary:
   1. Added `updateTopologyLineRows(rows, line, field, value)` in `lineTopologyUtils`.
   2. Replaced page-local `map + branch` logic in `updateTopologyLine` with helper call.
   3. Added unit tests for:
      - `line_name` trimmed update
      - `line_code` normalized upper-case update
   4. Preserved existing behavior (`S0`): update fields and normalization semantics unchanged.

6. Tests executed:
   1. `cd code/frontend && npm test -- --run`
   2. `cd code/frontend && npm run build`

7. Test results:
   1. `vitest`: `8 files passed / 63 tests passed / 0 failed`
   2. `vite build`: success

8. Risk and rollback:
   1. Low risk (pure update-helper extraction).
   2. Rollback by reverting the 3 files listed above.

9. Allow next step: `YES`
