# Refactor Test Record (2026-03-29)

1. Refactor step id: `BE-G-01` + `BE-G-02` + `BE-G-03`
2. Wave: `Wave-1 / PLAT-03`
3. Changed module: `api` + `module/platform` + quality gate script
4. Changed files:
   1. `code/backend/src/main/java/com/autoproduction/mvp/module/platform/ApiErrorResponseBuilder.java`
   2. `code/backend/src/main/java/com/autoproduction/mvp/module/platform/ContractRouteAuthService.java`
   3. `code/backend/src/main/java/com/autoproduction/mvp/api/GlobalExceptionHandler.java`
   4. `code/backend/src/main/java/com/autoproduction/mvp/api/ContractAuthFilter.java`
   5. `scripts/run_quality_gate.ps1`

## BE-G-01 (Unified Error Response + request_id pass-through)
5. Change summary:
   1. Added `ApiErrorResponseBuilder` as single builder for error payload shape.
   2. `GlobalExceptionHandler` now delegates all error response construction to builder.
   3. `ContractAuthFilter` unauthorized response now uses same builder.
   4. Behavior preserved: contract routes return top-level error fields; legacy routes keep nested `error` object.
   5. No route/contract break (`S0`).

6. Tests executed:
   1. `cd code/backend && mvn "-Dtest=MvpApiTest" test`

7. Test results:
   1. `MvpApiTest`: `38 passed / 0 failed / 0 errors`

## BE-G-02 (Contract route auth middleware hardening)
8. Change summary:
   1. Added `ContractRouteAuthService` for:
      - route auth applicability check
      - bearer header validation
   2. `ContractAuthFilter` now delegates auth decision logic to service.
   3. OPTIONS bypass and contract-route auth semantics unchanged.
   4. No route/contract break (`S0`).

9. Tests executed:
   1. `cd code/backend && mvn "-Dtest=MvpApiTest" test`

10. Test results:
   1. `MvpApiTest`: `38 passed / 0 failed / 0 errors`

## BE-G-03 (Cross-module quality gate entry)
11. Change summary:
   1. Extended root `scripts/run_quality_gate.ps1` with backend cross-module gate test entry.
   2. Added optional parameter `-SkipBackendTests` for faster artifact-only gate.
   3. Default gate now runs:
      - artifact checks
      - ERP extract template validation
      - backend test gate: `MvpApiTest`, `MvpStoreServiceOrderPoolMaterialsTest`, `ErpDataManagerTest`, `ErpSqliteOrderLoaderTest`, `SchedulerBenchmarkSmokeTest`

12. Tests executed:
   1. `powershell -ExecutionPolicy Bypass -File scripts/run_quality_gate.ps1`

13. Test results:
   1. Script passed end-to-end.
   2. Backend gate test result in script: no failures; `SchedulerBenchmarkSmokeTest` skipped (expected).
   3. Artifact checks returned warnings only (no errors).

14. Risk and rollback:
   1. Low risk; mostly service extraction and quality-gate wiring.
   2. Rollback by reverting the 5 files listed above.

15. Allow next step: `YES` (Platform Wave-1 BE-G chain complete for 01/02/03)
