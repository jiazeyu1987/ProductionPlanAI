# Refactor Test Record (2026-03-29)

1. Refactor step id: `FE-C-01` (slice-53)
2. Wave: `Wave-1 / PLAT-03`
3. Changed module: `frontend masterdata`
4. Changed files:
   1. `code/frontend/src/features/masterdata/lineTopologyUtils.js`
   2. `code/frontend/src/features/masterdata/lineTopologyUtils.test.js`
   3. `code/frontend/src/pages/MasterdataPage.jsx`

## FE-C-01 (Masterdata selected topology line sync helper extraction)
5. Change summary:
   1. Added `resolveSelectedTopologyLineCodeOnLineEdit(selectedTopologyLineCode, line, value)` in `lineTopologyUtils`.
   2. Replaced page-local inline comparison logic in `updateTopologyLine` with helper call.
   3. Added utility unit test for:
      - matched selected line update
      - non-matched line no update
      - empty current line code no update
   4. Preserved existing behavior (`S0`): selected line synchronization semantics unchanged.

6. Tests executed:
   1. `cd code/frontend && npm test -- --run`
   2. `cd code/frontend && npm run build`

7. Test results:
   1. `vitest`: `8 files passed / 66 tests passed / 0 failed`
   2. `vite build`: success

8. Risk and rollback:
   1. Low risk (pure helper extraction).
   2. Rollback by reverting the 3 files listed above.

9. Allow next step: `YES`
