# Refactor Test Record (2026-03-29)

1. Refactor step id: `FE-C-01` (slice-47)
2. Wave: `Wave-1 / PLAT-03`
3. Changed module: `frontend masterdata`
4. Changed files:
   1. `code/frontend/src/features/masterdata/lineTopologyUtils.js`
   2. `code/frontend/src/features/masterdata/lineTopologyUtils.test.js`
   3. `code/frontend/src/pages/MasterdataPage.jsx`

## FE-C-01 (Masterdata line-topology + section-leader prune state helper extraction)
5. Change summary:
   1. Added `applyLineTopologyWithSectionLeaderPrune(prevConfig, nextLineTopology)` in `lineTopologyUtils`.
   2. Replaced duplicated state assembly in:
      - `removeTopologyWorkshop`
      - `removeTopologyLine`
      - `removeLineTopologyRow`
   3. Added unit test to validate `lineTopology` update and `sectionLeaderBindings` pruning behavior.
   4. Preserved existing behavior (`S0`): same pruning rule (`line_code` normalized).

6. Tests executed:
   1. `cd code/frontend && npm test -- --run`
   2. `cd code/frontend && npm run build`

7. Test results:
   1. `vitest`: `8 files passed / 59 tests passed / 0 failed`
   2. `vite build`: success

8. Risk and rollback:
   1. Low risk (duplicate state-composition extraction).
   2. Rollback by reverting the 3 files listed above.

9. Allow next step: `YES`
