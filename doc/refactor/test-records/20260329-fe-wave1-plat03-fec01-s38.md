# Refactor Test Record (2026-03-29)

1. Refactor step id: `FE-C-01` (slice-38)
2. Wave: `Wave-1 / PLAT-03`
3. Changed module: `frontend masterdata`
4. Changed files:
   1. `code/frontend/src/features/masterdata/lineTopologyUtils.js`
   2. `code/frontend/src/features/masterdata/lineTopologyUtils.test.js`
   3. `code/frontend/src/pages/MasterdataPage.jsx`

## FE-C-01 (Masterdata section leader row match helper extraction)
5. Change summary:
   1. Added `matchesSectionLeaderBindingRow` in `lineTopologyUtils` to centralize `sectionLeader` row identity matching (`leader_id + line_code`, normalized).
   2. Replaced duplicated in-page matching logic in:
      - `updateSectionLeaderRow`
      - `removeSectionLeaderRow`
   3. Added utility unit tests for normalized match and mismatch cases.
   4. Preserved existing behavior (`S0`): same matching fields and normalization rules.

6. Tests executed:
   1. `cd code/frontend && npm test -- --run`
   2. `cd code/frontend && npm run build`

7. Test results:
   1. `vitest`: `7 files passed / 51 tests passed / 0 failed`
   2. `vite build`: success

8. Risk and rollback:
   1. Low risk (pure helper extraction + wiring replacement).
   2. Rollback by reverting the 3 files listed above.

9. Allow next step: `YES`
