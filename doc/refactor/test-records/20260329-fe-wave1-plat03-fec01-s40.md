# Refactor Test Record (2026-03-29)

1. Refactor step id: `FE-C-01` (slice-40)
2. Wave: `Wave-1 / PLAT-03`
3. Changed module: `frontend masterdata`
4. Changed files:
   1. `code/frontend/src/pages/MasterdataPage.jsx`

## FE-C-01 (Masterdata line-topology remove dedup)
5. Change summary:
   1. Replaced page-local `validLineCodes` + manual filter logic in `removeLineTopologyRow`.
   2. Reused existing helper `filterSectionLeadersByLineTopology(nextLineTopology, prev.sectionLeaderBindings)`.
   3. Preserved existing behavior (`S0`): same filtering criterion (`line_code` normalized upper-case).

6. Tests executed:
   1. `cd code/frontend && npm test -- --run`
   2. `cd code/frontend && npm run build`

7. Test results:
   1. `vitest`: `8 files passed / 54 tests passed / 0 failed`
   2. `vite build`: success

8. Risk and rollback:
   1. Low risk (duplicate logic replacement).
   2. Rollback by reverting the file listed above.

9. Allow next step: `YES`
