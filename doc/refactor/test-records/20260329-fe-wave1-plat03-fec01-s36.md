# Refactor Test Record (2026-03-29)

1. Refactor step id: `FE-C-01` (slice-36)
2. Wave: `Wave-1 / PLAT-03`
3. Changed module: `frontend masterdata`
4. Changed files:
   1. `code/frontend/src/features/masterdata/lineTopologyUtils.js`
   2. `code/frontend/src/pages/MasterdataPage.jsx`

## FE-C-01 (Masterdata line-topology matcher/code helper extraction)
5. Change summary:
   1. Added line-topology helpers:
      - `nextLineCodeFromTopology`
      - `matchesLineTopologyRow`
   2. Replaced page-local `nextLineCodeFromTopology` and duplicated line-topology row matching branches with helper calls.
   3. Preserved existing behavior (`S0`): line-code generation strategy and row-match normalization semantics unchanged.

6. Tests executed:
   1. `cd code/frontend && npm test -- --run`
   2. `cd code/frontend && npm run build`

7. Test results:
   1. `vitest`: `6 files passed / 49 tests passed / 0 failed`
   2. `vite build`: success

8. Risk and rollback:
   1. Low risk (pure helper extraction).
   2. Rollback by reverting the 2 files listed above.

9. Allow next step: `YES`
